version: '3'

services:
  linux-assistant:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: linux-assistant
    environment:
      - OLLAMA_HOST=http://localhost:11434
    network_mode: "host"
    stdin_open: true
    tty: true
